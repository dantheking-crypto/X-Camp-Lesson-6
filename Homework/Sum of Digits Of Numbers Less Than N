#include <iostream>
using namespace std;
int main() {
  int n;
  cin >> n;
  int sum = 0;
  for (int i = 1; i <= n; i++) {
    int current = i;
    while (current > 0) {
      sum += current % 10;
      current /= 10;
    }
  }
  cout << sum;
}
