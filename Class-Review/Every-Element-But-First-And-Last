#include <iostream>
using namespace std;
int main() {
  int length;
  while (cin >> length) {
    int numbers[length];
    for (int i = 0; i < length; i++) {
      cin >> numbers[i];
    }
    for (int i = 1; i < length - 1; i++) {
      cout << numbers[i] << " ";
    }
    cout << endl;
  }
}
